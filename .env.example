# =========================================================
# Environment Variables — Copy to .env file
# =========================================================

# Chrome Web UI (http://server:4100) login credentials
CHROME_USER=admin
CHROME_PASSWORD=your-secure-password-here

# API Key (optional — if set, all API calls require X-API-Key header)
# Leave empty for no authentication
API_KEY=
